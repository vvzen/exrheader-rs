name: Create Release

on:
  pull_request:
  push:
    branches: [ "main" ] # FIXME: Remove after I'm done testing!
    tags:
      - '**[0-9]+.[0-9]+.[0-9]+*'
env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: exrheader-rs-binary

jobs:
  build_and_upload_linux_x86:
    env:
      BINARY_SUFFIX: ${{env.RUNNER_OS}}-${{env.RUNNER_ARCH}}
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run tests
      run: cargo test --verbose
    - name: Run clippy
      run: cargo clippy
    - name: Build binary for ${{ env.BINARY_SUFFIX }}
      run: cargo build --release --verbose
    - name: Rename Binary
      run: |
        mkdir -p dist
        cp target/release/exrheader-rs dist/exrheader-rs-${{env.BINARY_SUFFIX}}
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.BINARY_NAME }}-${{ github.sha }}-${{ env.BINARY_SUFFIX }}
        path: dist/exrheader-rs-${{ env.BINARY_SUFFIX }}

  # TODO: Once I have more time, revisit matrix workflows
  # and remove this duplication.
  # https://docs.github.com/en/actions/how-tos/write-workflows/choose-what-workflows-do/run-job-variations
  build_and_upload_macos_arm64:
    env:
      BINARY_SUFFIX: ${{env.RUNNER_OS}}-${{env.RUNNER_ARCH}}
    runs-on: macos-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run tests
      run: cargo test --verbose
    - name: Run clippy
      run: cargo clippy
    - name: Build binary for ${{ env.BINARY_SUFFIX }}
      run: cargo build --release --verbose
    - name: Rename Binary
      run: |
        mkdir -p dist
        cp target/release/exrheader-rs dist/exrheader-rs-${{env.BINARY_SUFFIX}}
    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.BINARY_NAME }}-${{ github.sha }}-${{ env.BINARY_SUFFIX }}
        path: dist/exrheader-rs-${{ env.BINARY_SUFFIX }}

  release:
    needs:
      - build_and_upload_linux_x86
      - build_and_upload_macos_arm64

    runs-on: ubuntu-latest

    permissions:
      contents: write

    env:
      GH_TOKEN: ${{ github.token }}

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          fetch-tags: true
      # Fetch linux x86 binary
      - name: Fetch binary artifact
        uses: actions/download-artifact@v7
        with:
          pattern: ${{ env.BINARY_NAME }}-${{ github.sha }}-linux-x86
          path: dist
          merge-multiple: true
      # Fetch macOS arm64 binary
      - name: Fetch binary artifact
        uses: actions/download-artifact@v7
        with:
          pattern: ${{ env.BINARY_NAME }}-${{ github.sha }}-macos-arm64
          path: dist
          merge-multiple: true

      # Create a new release
      # Docs: https://cli.github.com/manual/gh_release_create
      - name: Create GitHub Release
        run: |
          tag=$(git describe --tags $(git rev-list --tags --max-count=1))
          gh release create $tag --generate-notes ./dist/* --fail-on-no-commits
